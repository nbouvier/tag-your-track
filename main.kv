#:kivy 2.0.0
#:import PlaylistScreen src.screens.playlist_screen
#:import TagListScreen src.screens.tag_list_screen
#:import HomeScreen src.screens.home_screen

MDBoxLayout:
    orientation: 'horizontal'
    MDBoxLayout:
        orientation: 'vertical'
        MDScrollView:
            MDBoxLayout:
                id: playlist_layout
                orientation: 'vertical'
                size_hint_y: None
                height: self.minimum_height
                BoxLayout:
                    orientation: 'horizontal'
                    size_hint_y: None
                    height: 40
                    FitImage:
                        source: ''  # Placeholder for thumbnail URL
                        size_hint_x: None
                        width: 40
                    MDLabel:
                        text: 'Loading playlists...'
                        size_hint_y: None
                        height: 40
    MDBoxLayout:
        orientation: 'vertical'
        size_hint_x: 0.7
        MDBoxLayout:
            id: tag_management_layout
            orientation: 'vertical'
            size_hint_y: None
            height: 50
        MDBoxLayout:
            id: songs_layout
            orientation: 'vertical'
            size_hint_y: None
            height: self.minimum_height

<MDScreenManager>:
    HomeScreen:
        name: 'home'
    PlaylistScreen:
        name: 'playlist'
    TagListScreen:
        name: 'tag_list'

<MDDropDownItem>:
    id: dropdown_item
    text: root.text
