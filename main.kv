#:kivy 2.0.0
#:import MainScreen src.screens.main_screen
#:import TagListScreen src.screens.tag_list_screen

MDBoxLayout:
    orientation: 'horizontal'
    MDBoxLayout:
        orientation: 'vertical'
        MDScrollView:
            MDBoxLayout:
                id: playlist_layout
                orientation: 'vertical'
                size_hint_y: None
                height: self.minimum_height
                BoxLayout:
                    orientation: 'horizontal'
                    size_hint_y: None
                    height: 40
                    FitImage:
                        source: ''  # Placeholder for thumbnail URL
                        size_hint_x: None
                        width: 40
                    MDLabel:
                        text: 'Loading playlists...'
                        size_hint_y: None
                        height: 40
    MDBoxLayout:
        orientation: 'vertical'
        size_hint_x: 0.7
        MDBoxLayout:
            id: tag_management_layout
            orientation: 'vertical'
            size_hint_y: None
            height: 50
        MDBoxLayout:
            id: songs_layout
            orientation: 'vertical'
            size_hint_y: None
            height: self.minimum_height

<MDScreenManager>:
    MainScreen:
        name: 'main'
    TagListScreen:
        name: 'tag_list'

<MainScreen>:
    # ...existing code...

<TagListScreen>:
    # ...existing code...

<MDDropDownItem>:
    id: dropdown_item
    text: root.text
